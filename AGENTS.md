# Workspace Rules

- After completing any task in this workspace, always output a summary and description of the changes made (like a commit message: concise summary line + detailed description).
- Do NOT automatically run `git commit`. Only output the summary and description in your final response.
- Always prefix CLI commands with `rtk` (see RTK.md: `rtk <command>`). Do not run raw commands.
